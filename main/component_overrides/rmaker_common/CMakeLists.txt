# Empty stub for espressif/rmaker_common. Same rationale as the
# esp_rainmaker stub next door — pulled in transitively by
# espressif/arduino-esp32 for non-c2/p4 targets but unused by BTClock.

idf_component_register()
