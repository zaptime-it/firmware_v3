# Empty stub for espressif/esp_diagnostics — pulled in transitively by
# espressif/arduino-esp32 (and by other rmaker-family components for
# esp_diagnostics + esp_insights) but unused by BTClock. See the sibling
# esp_rainmaker stub for full rationale.

idf_component_register()
